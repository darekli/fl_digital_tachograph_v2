# Docs

This folder contains repository documentation assets used by `README.md`.

## Structure

- `../assets/` - app screenshots used both by Flutter and `README.md`
- `config/` - safe example configs for documentation

## Notes

- Keep screenshot filenames stable so links in `README.md` do not break.
- Do not commit machine-specific secrets or absolute local paths in real config files.

